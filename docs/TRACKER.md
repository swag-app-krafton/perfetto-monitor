# Tracker

The live list of everything open on swagperf and on the performance of the app it measures. Bugs, features, tech debt and performance issues found in runs all live here, one row each. The product-manager skill (`.agents/skills/product-manager/SKILL.md`, shared by every coding agent; Claude Code runs it as the `product-manager` agent) keeps it current. After every recorded run it reviews the new data and opens or updates **performance issues for developer review** in `docs/issues/`. Any agent or person who finds, fixes, ships or defers something updates this file, or asks the product-manager agent to.

- Runs reviewed through: #2
- Last reconciled with git: d007f5a · 2026-09-24
- Feature notes: [FEATURES.md](FEATURES.md) · Testing plan for the 2026-09-24 changes: [TESTING-PLAN.md](TESTING-PLAN.md)

**How to review a performance issue:**
1. Open the issue's file and check the evidence and initial observations.
2. Set its `Status:` line to `confirmed`, `dismissed` or `expected`. For `dismissed` or `expected`, add a Log line with the reason and the value at the time.
3. Or tell the product-manager agent your decision and let it make the change.

The agent never closes an issue on its own.

**Statuses:**
- open: `needs-review` → `confirmed` → `in-progress`
- closed: `done`, `dismissed`, `expected`
- tool items can also be `planned` or `backlog`

**Priorities:**

| Priority | Tool items | Performance issues |
|---|---|---|
| P0 | Wrong data shown as right, or a security hole | none |
| P1 | Blocks a workflow | A high-severity signal seen in 2 or more runs, or an ordering violation |
| P2 | Friction | One run only, or medium severity |
| P3 | Polish | none |

---

## Performance issues from runs

Problems in the app under test, opened from runs by the automatic review. Each has a file in `docs/issues/`.

| ID | Title | App · path | Priority | Status | Seen |
|---|---|---|---|---|---|
| [P-001](issues/P-001.md) | Janky frames over its North Star target | Swag Pay · cold | P1 | needs-review | #72–#81 (5 of 10); #1 after the reset (1 of 1) |
| [P-002](issues/P-002.md) | Peak RAM usage over its North Star target | Swag Pay · cold | P1 | needs-review | #72–#81 (10 of 10); #1 after the reset (1 of 1) |
| [P-003](issues/P-003.md) | RAM growth over its North Star target | Swag Pay · cold | P1 | needs-review | #72–#81 (10 of 10); #1 after the reset (1 of 1) |
| [P-004](issues/P-004.md) | activity_create step regressed | Swag Pay · cold | P2 | needs-review | #72–#81 (1 of 10) |

## Tool bugs

| ID | Title | Area | Priority | Status | Raised | Evidence |
|---|---|---|---|---|---|---|
| B-002 | Peak RAM tile shows a green improvement while the Peak RAM gate fails | Overview | P2 | needs-review | 2026-09-23 | Overview, run #81: the Peak RAM tile shows "493 MB ▼ −5 MB vs 498 MB" in the good colour, because it compares against run #80. Beside it, the release gate shows Peak RAM 493 MB failing against its 320 MB limit. A reader sees "better" and "failing" for the same number. Source: `.claude/issues/image.png` |
| B-003 | A run's trailing baseline includes runs recorded after it, and mixes start paths | Regression detection (`store.baseline`) | P1 | needs-review | 2026-09-23 | `store.baseline()` takes the newest 20 runs of a step, excluding only the run being judged. It has no `id <` bound and no `path_kind` filter. At capture time, only earlier runs exist, so the verdict is right. Anything that recomputes an older run's regressions later (`swagperf reextract`, `swagperf triage` over an old range) judges it partly against runs that came after it, and cold and warm runs can share a baseline. Needs a developer to confirm the intended window before changing verdict logic. Related, fixed with F-013: `cli._analyse_run` passed the model baselines from every app and platform; it now scopes them by app, platform and simulator, as `regressions()` does. |
| B-004 | A trace recorded while Flashlight profiles the same phone is broken, and can be recorded as a normal run | Capture (`capture.py`, `jobs.py`) | P0 | in-progress | 2026-09-23 | Measured in the concurrency experiment ([result](flashlight-perfetto-observations.html), `experiments/flashlight-concurrency/`, 3 of 3 repetitions each way). If Flashlight's sampler starts during a Perfetto session, scheduler events, app slices, screen markers and RAM samples all stop at that moment and never resume. If Flashlight is already running, Perfetto starts without error but records no scheduler events, counters or process names. Perfetto logs no error either way. A manual stop records the run regardless (problems are only notes), so a partial or empty trace can reach history as a normal run. Fix: refuse to start a capture while `BAMPerfProfiler` or `atrace` is running on the device, and fail any trace whose scheduler events stop before it ends. That also catches Flashlight joining partway through, where the first part of the trace is still fine. Untested: whether the atrace settings Flashlight leaves behind after it exits also break the next capture. The raw experiment traces stay local (`results/` is git-ignored); the note has the numbers. P0 under the wrong-data rule. Must land before F-006. Implemented 2026-09-23, committed in e9d7162: `capture.require_no_other_profiler` before every capture and manual start, `extract.tracing_lost` on every device trace before it is recorded (jobs and CLI), and the dashboard's manual start respects the capture lock; checked against every Phase 0 trace. |
| B-005 | Seeded runs are recorded without an app, so they show as "Unknown app" but are judged against Swag Pay's budgets | CLI `seed` (`swagperf/cli.py`) | P3 | needs-review | 2026-09-24 | Found while building the run-perfetto-monitor skill (`.claude/skills/run-perfetto-monitor/`), which seeds a scratch history with `swagperf seed`. Repro: seed a history with `swagperf seed` and open the dashboard. Shown: the App picker lists the seeded runs as "unknown" and the run box reads "Unknown app 2.3.0", yet Startup draws "Budget 420 ms" and the Overview release gates judge them against Swag Pay's TTID budget (≤ 420 ms). Expected: the runs are labelled Swag Pay (`com.swag.pay`), because `synth.gen_trace` makes Swag Pay-shaped traces with `step:` markers, read through the instrumented `ex.extract()`. Cause: the `seed` branch of `swagperf/cli.py` calls `store.record(...)` (line 445) with no `app_pkg`. A run labelled as an unknown app is asserted against Swag Pay's budget, the "never assert our budgets on another app" pattern, although here the runs really are Swag Pay builds. Second effect: the App picker prefers `com.swag.pay`, so any real or synthetic `com.swag.pay` Perfetto run in the same history becomes the default view and hides the seeded series. Fix: record seed runs with `app_pkg="com.swag.pay"`, then update the Gotchas in the run-perfetto-monitor skill, which documents the current behaviour. Scope: `seed` and synthetic/demo data only; captured runs are not affected. P3 (polish): demo data only, the budget and verdicts are right for what the runs are, and the seeded runs stay reachable under "unknown" in the App picker. |
| B-007 | `triage --mark` can't move the marker back after a history reset | CLI `triage` (`swagperf/cli.py`) | P2 | needs-review | 2026-09-24 | Repro: after `swagperf reset`, `swagperf triage --json` reports `history_reset: true` and `through: 2`; `swagperf triage --mark 2` then prints "reviewed through #81" and leaves this file's marker at #81. Cause: the `--mark` branch of `cli.py` calls `triage.write_marker(n.mark)` without `force`, and `write_marker` never moves the marker backwards unless forced. The automatic review in `pm.py` passes `force=t["history_reset"]`, so only a manual review hits this. Effect: a manual review of a reset history can't close, and the next review sees the reset again and re-adds the same runs' evidence and reset notes. Seen 2026-09-24 in a manual review of runs #1–#2; the marker line was set to #2 by hand. Fix: pass `force` when the triage it follows reported `history_reset`, or add a `--force` flag. |
| B-009 | A run of a debuggable Android build is recorded and judged like any other, so a Swag Pay build carrying Killcam would be measured with Killcam's own work in it | Capture (`capture.py`) | P1 | needs-review | 2026-09-24 | Found while comparing swagperf with Killcam for F-016. The Android lane reads whether the app build is debuggable (`capture.py`, d43fe8a) and shows it in Run details. It never refuses a debuggable build or labels its run; the iOS lane refuses Debug builds. Killcam runs only in debug builds and installs first in `Application.onCreate`, "so startup itself (SoLoader, Hermes, the first frame) is on the timeline" (`SwagPayApplication.kt` and `debugtools/KillcamSetup.kt` in `~/Documents/swag-pay-killcam`, uncommitted). It also takes screenshots on screen changes, around taps and when the screen settles (Killcam README). F-012 already decided that screenshots cost CPU and would skew frames and TTID. So a capture of that build would be judged against Swag Pay's budgets with Killcam's startup and screenshot work inside it. Repro: unknown. Killcam has run in Swag Pay's debug build on the V2514 since about 15:02 on 2026-09-24 (Killcam's tracker, Host apps), but no swagperf capture of that build has been made, and Killcam's cost hasn't been measured (Killcam F-004). Also unknown: whether the Android runs behind P-001–P-004 were debuggable builds. Run details for those runs would say; a developer should check before the fix lands. Fix, agreed with Killcam's product manager on 2026-09-24 and waiting on the user's answer to Q3 (step 1 of the joint plan in [BACKLOG: one tool](BACKLOG.md#one-tool-killcam-and-swagperf-combined)): swagperf never judges a debuggable build, or a build whose trace carries Killcam's marker (Killcam F-016). Every capture refuses such a build unless the capture opts in. An opted-in run is its own kind, *debug build*, and names Killcam when the marker is there. It follows ADR 0002's simulator rules (no budgets, verdict capped at warn, no benchmark, skipped by triage) and never shares a baseline with other runs. The kind is read from the build details recorded at capture and from the trace, so `reextract` keeps it. Refusing needs only the debuggable flag; naming Killcam needs Killcam F-016. P1 because it blocks F-016 and Killcam F-004. Must land before `killcam-integration` merges into Swag Pay: from then on, a capture of that build is wrong data shown as right (P0). Update 2026-09-25: run #1's trace (`traces/manual_b073b3b4500c.pftrace`) has `package_list.debuggable = 1` for `com.swag.pay` (Run details had not recorded it). So run #1, the "#1 after the reset" evidence in P-001–P-003, was a debuggable build judged against Swag Pay's budgets. |
| B-010 | Swag Pay's startup is judged against the 420 ms camera-frame budget with a number that ends before the camera frame | Extraction (`extract.extract_any`, `extract._extract`) / catalogue (`apps.json`) | P0 | needs-review | 2026-09-25 | Found while planning camera metrics with Killcam's product manager. Repro: run #1 (`traces/manual_b073b3b4500c.pftrace`, Swag Pay, manual cold session on the V2514). History records it as derived (`derived = 1`) with startup 357.31 ms, and its only breaches are janky frames, peak RAM usage and RAM growth, so startup passes the 420 ms budget (`apps.json`, `ttid_ms`). In the same trace, `step:first_usable_camera_frame` lands 1219.3 ms after the start of the launch window `derive.derive_steps` uses, and the app's first camera frame event (`android.media.ImageReader#postEventFromNative`) lands at 1217.5 ms. Shown: a pass. Expected: time to first camera frame measured to the camera frame. Cause: `budgets.CRITICAL_PATH_RETURNING` ends at timed `step:camera_open` and `step:first_qr_decode`, which only `synth.py` emits. The app emits instant milestones only (`step:returning_bootstrap`, `step:first_usable_camera_frame`: `AppCoordinator.kt:37` and `:70` in `swag-pay`), so `extract_any` falls back to Android's launch phases, "time to initial display" (`extract.py:167-183`), and then checks the catalogue's 420 ms startup budget against it (`extract.py:202-218`). The camera step budgets (180 ms, 120 ms) have never been checked on a device. Fix options, waiting on Q-A: (a) swagperf ends Swag Pay's startup at the `step:first_usable_camera_frame` milestone, measured from the launch start in Android's own slices; (b) the app emits timed steps. Option (b) also needs `_extract` to measure from the launch start: it takes startup as the latest step end in absolute trace time (`extract.py:501`, `:521`), which works for synthetic and converted iOS traces that start near zero, but a device trace's clock is time since boot (run #1's launch window starts at 51,841,119.85 ms). Until a fix lands, a derived Swag Pay startup should not be checked against the camera-frame budget. Either fix changes TESTING-PLAN AND-01's expected result ("falls back to derived steps"). P0 under the wrong-data rule; a developer confirms. Step 1 of the joint camera order in [BACKLOG: camera](BACKLOG.md#camera-performance-swag-pays-scanner). Run #1 was a debuggable build (its trace's `package_list.debuggable = 1`; see B-009), so its numbers are a debug build's. The fault is in the extraction logic and holds for any build. On 2026-09-25 the user asked for Home's mount → first frame on every mount (F-019, F-020). That doesn't say where startup ends, so Q-A stays open. Not parked with the camera work on 2026-09-25: it is wrong data shown as right today, and the product-manager skill parks only work the user sets aside. The interim guard (don't check a derived Swag Pay startup against the camera-frame budget) doesn't need Q-A. The full fix does, so Q-A stays live while the other camera questions wait. Interim guard approved by the user on 2026-09-25 ("Include the guard") and in progress: one helper decides a run's startup target, a derived startup of an instrumented own app gets none, and the reason is shown where the target would be. B-010 stays open for the full fix (Q-A). Interim guard shipped in d2feed5: `budgets.startup_target` in `extract_any`, `_extract`, `server._payload`, `store.run_metrics` and the trend; old runs' startup breaches without a target are dropped when read; `ttid_target_reason` shown on Startup, Overview, Screens, the CLI and the Copilot. Tests: `tests/test_startup_target.py`. |

## Tool features

| ID | Title | Area | Priority | Status | Raised | Notes |
|---|---|---|---|---|---|---|
| F-001 | Copilot answers from a local Claude or Codex session | Copilot | P2 | planned | 2026-09-23 | An "Answer with" switch: Rules / Claude / Codex. It uses the logged-in CLI session, so no API key is needed. The rules engine still supplies every number, the model writes the answer, and numbers not found in the data are flagged. If the session fails, the rules answer is shown under a banner. T-001, which it needed first, shipped in d2feed5. |
| F-003 | Widget-level trace metrics (widget TTI) | Instrumentation / Screens | P3 | backlog | 2026-09-22 | [BACKLOG: widget TTI](BACKLOG.md#widget-level-trace-metrics-widget-tti) |
| F-004 | Measured A/B of graphify's token savings | Token monitor | P3 | backlog | 2026-09-22 | [BACKLOG: graphify A/B](BACKLOG.md#ab-measurement-of-graphifys-token-savings) |
| F-005 | Split the onboarding route into its nine steps | Swag Pay app model | P3 | backlog | 2026-09-22 | Partly addressed: trace markers exist per step. [BACKLOG: onboarding route](BACKLOG.md#onboarding-modelled-as-one-native-route) |
| F-006 | Flashlight audit lane: separate runs, results imported beside Perfetto runs | Capture / new `flashlight.py` | P2 | in-progress | 2026-09-23 | Step 3 of the [Flashlight note](flashlight-perfetto-observations.html), and its step 1: Perfetto owns every time-aligned metric; Flashlight owns only its score, report and thread summary. Flashlight never runs at the same time as Perfetto (step 2 of the note, the concurrency experiment, ruled it out). Runs `flashlight test` on its own, keeps `results.json` unchanged, stores a summary per audit. It must hold the capture lock, refuse while a manual session records, and reset atrace when it finishes, because Flashlight leaves its atrace settings behind. The standalone `flashlight` binary is Intel-only and will not run on Apple Silicon without Rosetta; the npm packages (`@perf-profiler/*`, last released 2024) run natively. Needs B-004 first. Implemented 2026-09-23, committed in e9d7162: `flashlight/` (pinned npm packages, `audit.js`, `summary.js`), `swagperf/flashlight.py`, `jobs.start_audit`, `/api/audits` and `/api/audit/start`, `swagperf audit run|list|show`, and a Profiler switch in the dashboard with Audit, Run audit and Audits pages. Not yet run end to end on the device. |
| F-007 | Per-thread CPU from the Perfetto trace, including the React Native JS thread | Extraction / Screens | P2 | planned | 2026-09-23 | Step 5 of the [Flashlight note](flashlight-perfetto-observations.html): Flashlight-style thread CPU (UI thread, RenderThread, `mqt_js`/`mqt_v_js`) computed from the trace's own scheduler data, so it lines up with screens. In a manual-mode trace on the V2514 the JS thread is named `mqt_v_js` (bridgeless) and resolves; still unchecked in a cold capture, whose config has no `task_newtask`/`task_rename`. Needs a React Native workload: Onboarding (`#rn`, first run only) or the offers screens (F-009). |
| F-008 | Flashlight audits and Perfetto runs in one view, labelled by provenance | Dashboard | P3 | dismissed | 2026-09-23 | Step 4 of the [Flashlight note](flashlight-perfetto-observations.html). Each audit is labelled `same_cohort` (same app build, device and flow) or `unrelated`, computed rather than typed in, and never drawn on the Perfetto timeline. Needs F-006. Dismissed 2026-09-23 by the user: the two profilers stay separate. The dashboard separates them with a Profiler switch instead of showing them in one view. |
| F-009 | React Native offer screens after Home in Swag Pay | Swag Pay app (`swag-pay` repo) | P2 | in-progress | 2026-09-23 | Home's banner cards open a React Native `Offers` surface: a collection page (Action games) and a gift card page per card (amounts with cashback, how to redeem, FAQs, related cards), modelled on swag.gg's gift card pages. Traced as `screen:Offers#rn` with sub-screens `Offers.collection` and `Offers.card`, and `offers_*` actions. Gives F-007 a React Native workload reachable without a first-run reset. Built and checked on the V2514 on 2026-09-23 (every marker present in a trace); not yet committed. Known limits: a page returned to by back starts from its first amount, and a rotation resets the app to Home (existing behaviour: navigation state is not saved across activity recreation). Card art now uses pictures already in the app for the Action games page, BGMI and Forza Horizon; the other four cards keep gradients because the repo has no pictures of those games. The Forza card reuses the picture from Home's "Forza Horizon" card, which is not Forza artwork (Home has the same mismatch). |
| F-010 | A 5,000-contact list built twice, React Native (FlashList) and Compose, for comparing scrolling | Swag Pay app (`swag-pay` repo) | P2 | in-progress | 2026-09-23 | The same deterministic contacts (grouped A–Z with sticky letter headers) on a React Native screen with Shopify's FlashList and a Compose `LazyColumn` screen, each its own route (`screen:ContactsRN#rn`, `screen:ContactsCompose#compose`) with a list-ready step marker, so Perfetto and Flashlight can compare the two runtimes on identical content. |
| F-012 | Screenshot testing with Maestro: a visual regression lane | Maestro / new lane | P3 | backlog | 2026-09-24 | Designed, then parked by the user on 2026-09-24 before any code. Maestro flows kept in swagperf take a screenshot at named checkpoints. swagperf compares each one with an approved baseline for that app, cohort and device class, fails the run on an unexpected change, and takes approvals in the dashboard. It's a separate lane from Perfetto runs, Android first. The product-manager skill's revisit queue says when to raise it again. [BACKLOG: screenshot testing](BACKLOG.md#screenshot-testing-with-maestro-visual-regression-lane) · [plan](screenshot-testing-plan.html) |
| F-013 | iOS lane: Instruments recordings read as Perfetto traces, simulator first | Capture / extraction / dashboard | P1 | in-progress | 2026-09-24 | `capture_ios.py` records the app with `xctrace` (os_signpost, os_log, Hangs, dyld Activity) on a booted simulator and `convert_ios.py` turns the recording into a Perfetto trace, so extraction, screens, history, regressions and Copilot read iOS runs unchanged. The app emits `os_signpost` markers through a cinterop wrapper (`swag-pay`). Runs carry `platform` and `simulator`; nothing mixes across them. Simulator runs are indicative only: no budgets, verdict capped at warn, no benchmark pin, skipped by triage. Dashboard: "Instruments · iOS" in the top bar's first control. Frames are unmeasured on the simulator. [ADR 0002](decisions/0002-ios-via-xctrace-normalised-to-perfetto.md) · [BACKLOG: beyond the simulator](BACKLOG.md#ios-lane-beyond-the-simulator) |
| F-014 | Hangs, JS exceptions with symbolicated stacks, and crashes, on both lanes | Extraction / app / dashboard | P2 | in-progress | 2026-09-24 | `stability.py` measures hangs by Apple's definitions (microhang 100–250 ms, hang ≥250 ms) from the Hangs instrument on iOS and top-level main-thread slices on Android, JS errors from the app's `error:js:` markers joined to their records (logcat via Perfetto's `android.log` on Android, os_log on iOS), and crashes (a fatal signal on iOS, the crash log on Android). App (`swag-pay`): `errorReporting.ts` installs a global handler and promise-rejection tracking; `SurfaceBoundary` wraps each React Native surface; `SwagErrors` chunks records into the platform log. Runs store `hang_count`, `longest_hang_ms`, `js_errors`, `crashed`; the analyst and triage raise them; a Stability screen in both lanes. Hermes stacks resolve against a build's source map (`swagperf maps add`, `sourcemaps.py`) through a pure-Python resolver checked against `metro-symbolicate`. Not yet exercised on a device: a real JS error from a React Native screen (verified by unit tests and synthetic traces). Resolver checked by hand on 2026-09-25, off device: Hermes stacks from four errors in the current Swag Pay Android release build (`surfaceRoute.ts:14`, `contactsDirectory.ts:150` and `:166`, `money.ts:3`) and from a new four-error test program each resolved to the line that throws, and agreed with `metro-symbolicate` on every frame except one Hermes-internal frame, which metro wrongly resolves to app code and swagperf leaves unresolved; a map from another build resolved nothing. No budgets yet. |
| F-015 | Killcam dashboard rebuilt on the swagperf design system (same UI/UX) | Killcam dashboard (`~/Documents/killcam/dashboard`) / design system (`frontend/src/design`) | P2 | needs-review | 2026-09-24 | Asked for by the user, as step 1 of F-016. Killcam's panels (Network, Mocks, Logs, Crashes, Replay, Flags, Remote Config, Storage, Device, Actions) are rebuilt from swagperf's design system and shell conventions. Killcam's API and wire contract (`dashboard/src/api/types.ts`) stay unchanged. Before Killcam `44ec8e0` its dashboard shared nothing with swagperf's: one hand-written stylesheet with an amber accent and system fonts, a hash router, and no tests or lint. The build session decided where the code lives: Killcam vendors swagperf's design system into `dashboard/src/design/` (locked at `d007f5a`) and wraps its own parts in `dashboard/src/kit/`, with bundled fonts and a phone layout. That replaces the earlier recommendation to build inside swagperf's `frontend/`. The rebuild is possibly shipped by Killcam `44ec8e0` (2026-09-24, "Rebuild the dashboard on the swagperf design system"; Killcam F-001); a developer or the user confirms. What's left here is step 11 of the joint plan in [BACKLOG: one tool](BACKLOG.md#one-tool-killcam-and-swagperf-combined): take `kit/`'s generic parts (screenshot frame and player, JSON tree, split view, virtualised list, stack trace view, key-value editor) upstream into `frontend/src/design` and onto `/design-system`, each meeting T-003's lint rule and showcase test. Killcam T-008 adds a check that flags local edits to its copy and drift from the locked commit. The vendored copy stays until the user answers Q2 and Q7. [BACKLOG: Killcam on the design system](BACKLOG.md#killcam-on-the-swagperf-design-system) |
| F-016 | One tool: Killcam and swagperf combined | Shell / server / Killcam (`~/Documents/killcam`) | P2 | needs-review | 2026-09-24 | Asked for by the user, after F-015. Killcam, the in-app debugging tool for Swag Pay's debug builds, becomes a lane beside Perfetto · Android, Instruments · iOS and Flashlight · Android. Planned jointly with Killcam's product manager on 2026-09-24 (Killcam F-003): a 12-step plan in the BACKLOG write-up. Two servers: the Killcam lane embeds Killcam's own dashboard (F-017, Killcam F-017), and swagperf's server never forwards to the phone; it only reads link keys (F-018). Links join the lanes by screen name, JS-error identity and app version, never by timestamp, because the two tools never measure the same process. Both tools already read Swag Pay's `SwagTrace` markers and `SwagErrors` records; T-005 (Killcam T-014) gives them one definition. Must land before `killcam-integration` merges into Swag Pay: B-009 and Killcam T-007. Waiting on the user: Q1–Q7 and Q9, each with a joint recommendation in the BACKLOG write-up. [BACKLOG: one tool](BACKLOG.md#one-tool-killcam-and-swagperf-combined) |
| F-017 | Killcam lane: Killcam's own dashboard embedded in swagperf's shell | Shell / launcher (`perfetto_init`, `doctor`) | P2 | needs-review | 2026-09-24 | Step 8 of the joint plan in [BACKLOG: one tool](BACKLOG.md#one-tool-killcam-and-swagperf-combined). A fourth choice in the top bar's first control shows Killcam's dashboard from the phone (`127.0.0.1:8090` over `adb forward`), embedded, not rebuilt and not proxied, so Killcam's own `Host` check and `X-Killcam` write rule stay in charge. `perfetto_init` sets up the port forward, and `doctor` reports whether Killcam is running in the app on the device by asking its `/api/status`. Follows the user's F-008 decision: lanes stay separate behind a switch. Needs Killcam F-017 (its dashboard lets only loopback pages embed it) and the user's answer to Q1. Settle the lane-switch bug first: switching lanes is suspected of losing the Android app filter (TESTING-PLAN IOS-D7, S-09), and a fourth lane makes it worse. |
| F-018 | Links between Killcam sessions and swagperf runs, by screen name, JS-error identity and app version | Server / Stability / Screens | P2 | needs-review | 2026-09-24 | Step 9 of the joint plan in [BACKLOG: one tool](BACKLOG.md#one-tool-killcam-and-swagperf-combined). Stability links to the Killcam sessions with the same JS error, and a Screens row to the Killcam sessions on that screen; Killcam's crashes and screens link back (Killcam F-018). swagperf's server reads only `GET /api/sessions`, `/api/crashes` and `/api/info`, when a page is viewed, and keeps only session ids, screen names, the JS-error identity (`error:js:<source>:<Name>`) and the app version. It never calls `/api/timeline` or `/api/sessions/{id}`, which hold bodies, logs and tap text; it never writes; it stores nothing in `history.db`; the Copilot sees none of it. Links read "same error", never "same crash site": a debug build loads its JS from Metro, so a release source map never fits it (F-014 refuses a map from another build). Needs T-005, F-017 and Killcam F-018 (T-001 shipped in d2feed5), which adds `SessionSummary.screens`. If Killcam T-001 adds a loopback secret, this read must carry it. Later extension, agreed with Killcam's product manager on 2026-09-25: Killcam's camera events link to swagperf's Camera page (F-020) by screen and app version, never by timestamp ([BACKLOG: camera](BACKLOG.md#camera-performance-swag-pays-scanner)). |
| F-019 | Camera markers in Swag Pay's scanner: the `camera:` family | Swag Pay app (`swag-pay` repo) | P2 | backlog | 2026-09-25 | Built by Swag Pay; step 3 of the joint camera order. Android first, in `CameraPlatformSurface.android.kt`: `camera:open`, an async slice with a new cookie, from Home's mount on every mount (the scanner's first composition, where `session.start()` is called from the `AndroidView` update block, as the user asked on 2026-09-25: "On home page mount/render, the camera should auto start, and the time to first frame should be captured"), and from CameraX's restart after the app returns from background, to the first analysed frame, with `firstFrameDelivered` reset per open. Today the camera already starts on Home (`CameraHomeScreen.kt:239`), but `firstFrameDelivered` never resets and `step:first_usable_camera_frame` fires once per launch, on the returning-user path only; `camera:decode`, an async slice with its own cookie, from ML Kit's `scanner.process` to `addOnCompleteListener` (at most one in flight, and one per 250 ms by the app's own throttle); `camera:qr_found`, an instant with no value; `camera:open_failed#denied\|unavailable\|closed`, where `closed` means the camera closed before its first frame: the app ends `camera:open` at close and emits it, and both tools read that open as "no first frame", never as a duration (agreed with Killcam's product manager on 2026-09-25); `camera:close#dispose\|background\|permission`, an instant, once per open. Fixed names and outcome classes only: the decoded QR (payee VPA, name, amount) never goes into a marker. iOS later: `camera:open` needs a new async signpost family (`swagsignpost.def`, `convert_ios.ASYNC_FAMILIES`), and per-frame decode can't be measured there, because `AVCaptureMetadataOutput` decodes inside AVFoundation. Needs T-005. [BACKLOG: camera](BACKLOG.md#camera-performance-swag-pays-scanner) Parked by the user on 2026-09-25 ("Just keep those as backlog tasks, to be done after we have another app which will be the focus"); revisit when another app is integrated with Killcam and the user makes it the focus. |
| F-020 | Camera metrics from the trace: first frame per open, time to decode each frame, QR resolve time, RAM usage while the camera is open | Extraction (new `swagperf/camera.py`) / store / dashboard | P2 | backlog | 2026-09-25 | Step 4 of the joint camera order. A module like `stability.py`. `camera:open` durations (a slice that never closed is "no first frame", never a duration); `camera:decode` count, first decode, p50, p90 and max in ms, and decodes against camera frames delivered (`android.media.ImageReader#postEventFromNative`); QR resolve as `camera:qr_found` → `action:qr_validate_*` → `screen:Pay#compose`; RAM usage (`mem.rss`, `mem.rss.anon`, `mem.rss.file`) before `camera:open`, the peak while open, and after `camera:close`. A Camera page in both lanes, Compare rows and CLI. Per scan, as the user asked on 2026-09-25 ("when it registers a QR, it should log the metrics"): one row per `camera:qr_found`, shown on the Camera page and printed by the CLI. Each row has Home's mount → first frame for its open, first frame → QR found, Home's mount → QR found, QR found → validated, QR found → Pay screen shown, decodes so far with decode p50, p90 and max so far in ms, and the outcome class. The two "→ QR found" figures measure the tester's aim unless a test QR is in view from the start (Q-E). Derived runs get only camera open (CameraX's `CX:bindToLifecycle*` → the first `ImageReader` event in the app's own threads) and RAM usage over it, labelled derived; decode and QR resolve are "not measured" for them. Never a Swag Pay budget on another app. No camera budgets until a 10-session baseline and Q-B, and no triage signals until then. The trace processor swagperf uses (v49) has no camera module (`android.camera` doesn't exist), so everything is SQL over slices and counters. Needs F-019, B-009, B-010. [BACKLOG: camera](BACKLOG.md#camera-performance-swag-pays-scanner) Parked by the user on 2026-09-25 ("Just keep those as backlog tasks, to be done after we have another app which will be the focus"); revisit when another app is integrated with Killcam and the user makes it the focus. |
| F-021 | Camera cycle test: N camera open/close cycles in one process, RAM usage per cycle, and a "possible leak" signal from the trend | Capture / jobs (`capture.py`, `jobs.py`) | P2 | backlog | 2026-09-25 | Step 6 of the joint camera order. One trace across N cycles, driven over adb, refusing debuggable builds (B-009). RAM usage is read at the same point in each cycle (before each `camera:open`, after each `camera:close`), the first cycle is skipped as a warm-up, and the result is a trend in MB per cycle. A leak is never claimed from one trace or one cycle: stress tests can't show one (each session is a new process), and a session's RAM growth isn't one (run #1: 414.4 MB across startup and every screen). Also a sustained-scan mode on Home only, which reuses thermal drift and records what the camera does while the scanner looks closed (it stays on: `CameraHomeScreen.kt:239`, `enabled = true`). Needs F-020 and Q-C. [BACKLOG: camera](BACKLOG.md#camera-performance-swag-pays-scanner) Parked by the user on 2026-09-25 ("Just keep those as backlog tasks, to be done after we have another app which will be the focus"); revisit when another app is integrated with Killcam and the user makes it the focus. |
| F-022 | Heap-dump runs that confirm a camera leak | Capture / extraction | P3 | backlog | 2026-09-25 | Step 8 of the joint camera order. A run kind of its own with Perfetto's `android.java_hprof`: dumps after the first and last cycles of F-021, and the reachable instances and size of the camera classes (`AndroidCameraSession`, `PreviewView`, `ImageProxy`, ML Kit's scanner) from `heap_graph_object` and `heap_graph_class`. A dump pauses the app, so the run is never timed, has no budgets and never joins a baseline. Needs a profileable release build (`<profileable android:shell="true"/>`, Swag Pay's part; Q-D), because the V2514 runs a user build and a debuggable build is never judged (Q3). Release has `isMinifyEnabled = false` today, so class names are readable; turning on R8 would need a mapping file. Complements Killcam F-020 (LeakCanary in debug builds). Needs F-021 and Q-D. Q-D answered by the user on 2026-09-25 ("Create a flavor for the release build that remains profilable"). Swag Pay builds a separate profileable variant of the release build; the store release stays unchanged. Killcam's constraints on it, also in Killcam T-007: it depends on `killcam-no-op` like release (`releaseImplementation` doesn't apply to a new build type, so it needs its own no-op dependency or it won't compile), it gets T-007's release stub of the bridge, and it carries no LeakCanary. swagperf tells it apart through `package_list.profileable_from_shell` in the trace, and a heap-dump run refuses a build that isn't profileable. Whether every other swagperf run also uses the variant is the new Q-H in the BACKLOG. Recommendation: heap-dump runs only, until a 10-session stress test of each build shows the variant runs like the store release. Now needs F-021 and Swag Pay's variant. [BACKLOG: camera](BACKLOG.md#camera-performance-swag-pays-scanner) Parked by the user on 2026-09-25 ("Just keep those as backlog tasks, to be done after we have another app which will be the focus"); revisit when another app is integrated with Killcam and the user makes it the focus. |
| F-025 | Test-flow runs: a scripted flow inside one Perfetto session, recorded as a run with its screens, actions, JS errors and crashes | Capture / jobs (`capture.py`, `jobs.py`) / a flow runner | P2 | in-progress | 2026-09-25 | Work approved by the user on 2026-09-25 ("Yes start working"); designed after F-028. Asked for by the user on 2026-09-25: test suite runs where "not just the cold/warm start but also custom test flows will be run and the entire perfetto trace will be captured along with the screen traces and action markers". [ADR 0001](decisions/0001-nlp-mobile-automation-ci.md) plans this (Maestro runs a frozen plan "while one Perfetto session captures the run"), but c16d23d shipped only the plan. The nearest thing today is a manual session driven by hand (`swagperf manual start [--cold]`, then `swagperf manual stop`). Its config writes the whole session to a file on the phone and records the screen and action markers and the app's error log. New work: run a named flow from a cold or warm start inside that session and stop when the flow ends. Record the run with the flow's name, whether it passed and its first failed step. Keep the trace and record the run when the flow fails, because a crash is a result. Give each flow its own path kind, so a flow run never shares a baseline with cold or warm starts (B-003). Stability (F-014) needs nothing new: both Android configs carry the error log (`capture.LOG_SOURCE`), and every extraction path reads stability. A relaunch after a crash is covered too, because every process of the package is read. Limits that matter more in long flows: `crashed` is one flag holding the first crash line, so a flow that crashes twice reports one crash. F-014's device checks are still open: a real JS error (TESTING-PLAN STB-13) and a native crash, whose tombstone lines come from `crash_dump`, outside the app's process (STB-14). Watch-outs: Maestro's device server hangs on the Vivo after Store→Rewards, so the POC drives its scenarios with raw ADB taps (BACKLOG, F-012 "Watch out for"). A flow inside a measured run takes no screenshots (F-012's decision). Starting this work fires F-012's revisit trigger. |
| F-028 | ANRs, and one dashboard tab for JS exceptions, ANRs and crashes | Extraction (`stability.py`) / store / dashboard | P2 | in-progress | 2026-09-25 | Asked for by the user on 2026-09-25: "in the perfetto dashboard there should be a dedicated option for the JS exceptions, ANRs and Crashes, we can club all these three metrics into one tab only". Built and committed 2026-09-25 in `58fe6fa`…`d21da60` ([spec](superpowers/specs/2026-09-25-crashes-anrs-tab-design.md), [plan](superpowers/plans/2026-09-25-crashes-anrs-tab.md)): ANRs from the trace processor's `android.anrs` module (the `ErrorId`/`Subject` counters system_server writes under the `am` category), each with its type, reason, screen and the main thread's longest slice before it; every crash with its crash log (AndroidRuntime reports put back together; native tombstones read by their `>>> <pkg> <<<` header, which closes STB-14's gap); runs gain `anr_count` and `crash_count`; an ANR is high severity; triage opens `anr:<type>` and `crash:<signature>` signals with their own context (S-02 fixed). The Stability tab became Crashes & ANRs (`/crashes`; `/stability` redirects), one time-ordered list with a kind filter, and hangs moved to Frame pacing. A trace recorded without the log source or the `am` category reads "not measured", never 0 (run #1 has no log source). Left: the phone check, TESTING-PLAN CR-07 (a real ANR, a Kotlin/Java crash and a native crash on the V2514). The phone was disconnected all of 2026-09-25, so it's unchecked whether the V2514 writes the ANR counters; if it doesn't, the plan's Task 4 adds the event-log fallback. Serves F-025's flow runs. |
| F-029 | Non-fatal Kotlin/Java exceptions the app catches | App (`swag-pay`) / extraction (`stability.py`) | P3 | needs-review | 2026-09-25 | Found while answering whether F-014 covers test-flow runs; logged at the user's request on 2026-09-25. F-014 records React Native JS errors (marker plus record) and fatal crashes only. An exception the native code catches and survives leaves nothing in the trace. It would need the app to report it the way `SwagErrors` reports JS errors, e.g. an `error:native:` marker plus a record, which is T-005's contract. |

## Tech debt & security

| ID | Title | Area | Priority | Status | Raised | Notes |
|---|---|---|---|---|---|---|
| T-002 | Navigation stack depth is replayed, not recorded | Screens | P3 | backlog | 2026-09-22 | [BACKLOG: stack depth](BACKLOG.md#stack-depth-is-reconstructed-not-recorded) |
| T-004 | Frame deadline is hard-coded at 60 Hz | Extraction (`FRAME_NS`) | P2 | planned | 2026-09-24 | `budgets.FRAME_NS` is 16.67 ms, so on a 90/120 Hz Android phone or a 120 Hz ProMotion iPhone every frame between one refresh and 16.67 ms counts as on time. Frames should be judged against their own deadline: the display's refresh rate on Android (`refresh_hz` is already recorded), each frame's expected interval from Hitches on iOS. |
| T-005 | Two readers of `SwagTrace` and `SwagErrors`: one definition and one shared fixture | Extraction (`stability.py`, `screens.py`) / contract in `swag-pay` | P2 | needs-review | 2026-09-24 | Step 4 of the joint plan in [BACKLOG: one tool](BACKLOG.md#one-tool-killcam-and-swagperf-combined). The same item as Killcam T-014, owned by the `swag-pay` repo. swagperf reads the markers and error records from the trace. Killcam's `KillcamTraceBridge` (uncommitted, in `~/Documents/swag-pay-killcam`) reads them inside the app: it sees `screen:`, `action:` and `step:` markers, skips `nav:` and doesn't forward timed spans. Fix: one definition in `swag-pay`, and one fixture both readers test against. The fixture includes the `error:js:<source>:<Name>#fatal\|nonfatal@<id>` identity, which Killcam stores as a crash's `type` and F-018 joins on. swagperf's part: its readers' tests use the shared fixture. Land it before the bridge is committed. Camera, agreed with Killcam's product manager on 2026-09-25 (step 2 of the joint camera order in [BACKLOG: camera](BACKLOG.md#camera-performance-swag-pays-scanner)): the definition and the fixture gain the `camera:` family (`camera:open`, `camera:decode`, `camera:qr_found`, `camera:open_failed#…`, `camera:close#…`; F-019). Killcam's bridge never forwards `camera:decode` as one event per frame, and Killcam T-014 states the same. The camera part is parked with F-019–F-022 (by the user, 2026-09-25); the rest of T-005 stays open as it was. |
| T-006 | The chart palette fails the dataviz validator: `--c4` is too close to `--c2`, and the dark theme's series colours sit outside the lightness band | Design system (`frontend/src/design/foundations/tokens.css`) | P3 | needs-review | 2026-09-25 | Found while building F-026, with the dataviz skill's `validate_palette.js --pairs all`. Light theme (`--c1..--c4` = `#2463d9,#d55f14,#0b8577,#9e7609` on `#ffffff`): `--c4`↔`--c2` ΔE 1.7 for protanopia and 10.9 for full colour vision (floor 15), and `--c3` has chroma 0.097 (floor 0.1). Dark theme (`#5b9bff,#ff8f45,#2ed1be,#e8c14e` on `#141414`): all four outside the lightness band (0.69–0.82), and `--c4`↔`--c2` ΔE 12.1 normal, 6.2 deutan. `--c1..--c3` pass separation in both themes, so the Trend page uses only those (3 devices). Every chart with four series, and `--c4` as the benchmark colour next to `--c2`, is affected. Fix: re-step `--c4` (and the dark ramp) with the validator, check both themes, and look at every chart that uses them. |

## Done (last 30 days)

| ID | Title | Commit | Date |
|---|---|---|---|
| T-001 | Dashboard server refuses other websites: a loopback `Host`, a loopback `Origin` when one is sent, and `X-Swagperf: 1` on every write (P0) | d2feed5 | 2026-09-25 |
| F-023 | AI summary for every run: Overview's Generate summary, a switch on Capture, Stress and Manual, `swagperf summary RUN`; it never changes the verdict | d2feed5 | 2026-09-25 |
| F-024 | The AI summary compares the run with its pinned benchmark: the model's words and Compare's tables, kept as written | d2feed5 | 2026-09-25 |
| F-026 | Trend view: each metric and launch step across app versions, a line per device, each device's pinned benchmark dotted | d2feed5 | 2026-09-25 |
| F-027 | "Budget" is "North Star target" in everything a reader sees; the per-frame budget is the frame deadline | d2feed5 | 2026-09-25 |
| B-001 | Verdict headline has units and one bracket group: "493.3 MB vs 320 MB (+54.2%)", then ", and 1 more finding" | with F-027 · d2feed5 | 2026-09-25 |
| — | Run skill: drive the dashboard headless on scratch data (`.claude/skills/run-perfetto-monitor/`) | 1d6b4fb | 2026-09-24 |
| B-006 | Per-screen CPU read 0 ms when a trace had no scheduler data; now unmeasured (`None`), shown as "–", and summaries no longer add it as 0. Found through the iOS lane, whose traces have no scheduler data; it hit Android traces without sched_switch too (P0) | with F-013 | 2026-09-24 |
| B-008 | `triage` never reported step-based issues as quiet: the quiet check read a signal key's app with the subject's colon in it (`regression:step:…`). Found in the 2026-09-24 review before the F-013 change was committed; keys are now read from the end (`triage._key_scope`), with a test for step and iOS keys | with F-013 | 2026-09-24 |
| T-003 | Dashboard pages built only from the design system: the three hand-drawn charts (Stress box plot, Startup ordering, Memory breakdown) and the two hand-built expandable tables now use `DotBoxPlot`, `SpanTimeline`, `StackedBars` lg and `ExpandableTable`; pickers use `Table`/`SortTh`/`RowAction`; no inline styles or raw controls in pages, enforced by oxlint; every component on `/design-system`, enforced by a test | 9ca753a | 2026-09-24 |
| — | Flashlight + Perfetto on one device (step 2 of the note): tested and ruled out, harness in `experiments/flashlight-concurrency/` | 2e6ce6e | 2026-09-23 |
| F-011 | Plain-language descriptions for step names and thread names (`STEP_DESCRIPTIONS` in `budgets.py`, `threads.py`, `Term`/`TermList`) | e9d7162 | 2026-09-23 |
| — | Reset command, a Version filter, and a Run list sortable by any metric | 7d36827 | 2026-09-23 |
| — | One run in view: a Run picker in the top bar, and the run on every screen | ff18ada | 2026-09-23 |
| — | Each run records where it was measured: device, device state and app build | d43fe8a | 2026-09-23 |
| F-002 | Tracker, product-manager agent and automatic run review | 7cddefc | 2026-09-23 |
| — | README: quick start, full instructions, what to monitor per screen | 8d997d8 | 2026-09-23 |
| — | NLP mobile automation in CI: architecture plan, ADR 0001, skill | c16d23d | 2026-09-23 |
| — | Old dashboard removed; every static path kept inside the build | 38f56cd | 2026-09-23 |
| — | Dashboard design system and the Copilot panel | a7e66df | 2026-09-23 |
| — | Copilot engine and API: streamed answers, threads, feedback, pins | 04c2129 | 2026-09-23 |
| — | Dashboard redesign in React + TypeScript (all screens) | 51f403f…b46f5fe | 2026-09-23 |
| — | Device-trace accuracy: RAM and frames scoped to the app | a792525 | 2026-09-23 |
| — | Manual trace config slimmed from ~230 MB/min to ~53 MB/min | feb61cc | 2026-09-23 |
| — | Live markers read incrementally | f190cda | 2026-09-23 |
| — | Screens: session timeline, per-screen app jank, transition costs | db78f77 | 2026-09-23 |
| — | Live token-consumption monitor | 0aabe16 | 2026-09-22 |
| — | Manual tracing mode and per-screen CPU/RAM attribution | d4b18b3 | 2026-09-22 |
| — | Stress tests: repeated cold starts, per-session view | 7ddf1b9 | 2026-09-22 |
| — | Any Android app via step derivation, plus a competitor catalogue | 377aa0d | 2026-09-22 |
| — | Run comparison and pinned benchmark runs | 88ba98d | 2026-09-22 |

## Archive

Done items older than 30 days, as `ID · title · commit`.
