import s from './DataTable.module.css'

/** Class for a right-aligned figure cell (<th>/<td>) in a TableCard table. */
export const numCell = s.num!

/** Class for the row a table is focused on (the plotted test, the open run). */
export const selectedRow = s.selected!
